'use client';

import TextareaAutosize, { TextareaAutosizeProps } from 'react-textarea-autosize';

export default function AutosizeTextarea(props: TextareaAutosizeProps) {
    return <TextareaAutosize {...props} />;
}
